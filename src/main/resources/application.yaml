jwt:
  secret: javaspringboot2026poskitadeveloperscom
spring:
  application:
    name: pos
  datasource:
    url: jdbc:postgresql://localhost:5432/pos_db
    username: okidwijaya
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


